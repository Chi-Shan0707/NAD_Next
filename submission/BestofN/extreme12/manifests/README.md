# Extreme12 Manifests

Metadata and provenance for exported Extreme12 submission artifacts.

- `nogithub_detailed_qwen3_lcb_from_rotation_adapter_late70100_manifest.json`: provenance for the detailed `nogithub` variant that overrides only `Qwen3-4B/lcb_v5` from the rotation-adapter slot100 export.
