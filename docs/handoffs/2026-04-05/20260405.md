# 2026-04-05 Handoff Notes

> Status update (later on 2026-04-05):
> - This pre-validation handoff is now superseded by:
>   - `../../CODE_SELECTOR_VALIDATION_20260405.md`
>   - `./20260405_code_selector_handoff.md`
> - Phase-2 follow-up is now recorded in:
>   - `../../CODE_BASELINE_V1_PHASE2_20260405.md`
>   - `./20260405_code_baseline_v2_planning_handoff.md`
> - The code-oriented selector slice was completed and committed in:
>   - `e725687` — `selectors: optimize code-oriented dynamic plugin`
> - Final coding-cache ranking result on `livecodebench_v5`:
>   - `PrefixSaturationSelector`: `SelAcc@10 = 61.70%`, `Pairwise = 51.27%`, `Hit@1 = 59.28%`
>   - `tournament-copeland`: `SelAcc@10 = 59.27%`, `Pairwise = 49.92%`, `Hit@1 = 58.08%`
>   - `min-confidence`: `SelAcc@10 = 52.15%`, `Pairwise = 49.51%`, `Hit@1 = 59.88%`
> - Phase-2 best guarded coding config:
>   - `reflection_threshold = 0.30`
>   - `reflection_lookback_slices = 16`
>   - `prefix_fraction = 0.30`
>   - `SelAcc@10 = 63.33%`, `Pairwise = 50.67%`, `Hit@1 = 61.08%`
> - Experimental coding-only submission patch:
>   - `submission/BestofN/extreme12/patches/extreme12_baseline12_pointwise_best_only_ref030_t1024_scale100_rank__code_phase2_candidate_thr030_lb16_pf030_lcb_patch.json`
> - The original notes below are kept as historical context for the pre-validation state.

## 1. Current high-level state

This repo already has a completed analysis write-up in:
- `../../HYPOTHESIS_ANALYSIS_20260405.md`

That doc's main conclusions are the currently accepted direction:
- H1 supported
- H2 supported
- H3 only weakly supported
- H4 partially supported
- Do **not** prioritize graph-topology expansion right now
- Treat coding as a **feature-design** problem first
- Prioritize dynamic structure features: prefix / tail / reflection / recovery / local stability
- Keep `baseline12_pointwise` as the control group

I also pushed that analysis doc to GitHub already:
- commit: `9ad5498`
- branch: `main`
- remote pushed: `origin/main`

## 2. What was being implemented right before handoff

We switched from a broad plan to a **selector-first** implementation slice.

The intended scope for this slice is:
- implement a code-oriented selector only
- do **not** refactor `earlystop_v3` yet
- do **not** resume `Extreme10`
- do **not** broaden into graph features
- optimize a new coding selector around:
  - prefix best-window quality
  - head-tail settle gap
  - reflection density
  - tail variance
  - post-reflection recovery

## 3. Files currently involved

### Newly created / modified in local worktree
These are **not committed yet** unless explicitly noted.

- `nad/core/selectors/code_dynamic_impl.py`
  - New shared helper module for code-oriented dynamic features
  - It currently contains:
    - raw feature extraction
    - group-rank feature builder
    - score-series helper for prefix-position scoring
  - Feature directions encoded there:
    - `prefix_best_window_quality`: lower is better
    - `head_tail_gap`: higher is better
    - `reflection_density`: lower is better
    - `tail_variance`: lower is better
    - `post_reflection_recovery`: higher is better

- `plugins/prefix_saturation_selector.py`
  - Refactored away from a standalone heuristic
  - Now uses `nad.core.selectors.code_dynamic_impl`
  - Still plugin-based; not promoted to built-in registry
  - Uses weighted combination of rank-normalized dynamic features
  - Tie-break remains medoid-style on `D`

### Important file intentionally left unchanged so far
- `nad/ops/earlystop_v3.py`
  - Still on the old trajectory-only coding route
  - I explicitly did **not** finish wiring it to the new shared core in this slice
  - If you open a new window, do **not** let Codex drift into refactoring this unless you consciously expand scope

### Existing analysis / benchmark files you should read first
- `../../HYPOTHESIS_ANALYSIS_20260405.md`
- `docs/EXTREME12_TEST_ANALYSIS.md`
- `docs/EXTREME12_V2_EXPERIMENT.md`
- `results/scans/lcb/lcb_knn_scan.json`
- `results/extreme8_experiments/20260402_112323/summary_20260402_112323.json`
- `result/tmp_rank_positions/selector_rankings_20260405_023040.json`

## 4. What is already known from implementation attempts

### Confirmed
- `nad/core/selectors/code_dynamic_impl.py` exists in the worktree
- `plugins/prefix_saturation_selector.py` exists in the worktree
- Syntax check succeeded earlier with:
  - `python3 -m py_compile nad/core/selectors/code_dynamic_impl.py plugins/prefix_saturation_selector.py`
- Package import for `nad.core.selectors.code_dynamic_impl` succeeded
- The full-LCB validation command was started but intentionally not completed before handoff

### Not completed yet
- No finished selector-vs-baseline accuracy report for the new plugin yet
- No committed implementation commit for the selector work yet
- No push for the selector work yet
- No final smoke-test / evaluation summary for the new plugin yet

## 5. Important operational constraints

### CPU / parallelism
Repo guidance says:
- keep `THREADS × PARALLEL_JOBS <= 16`

Interpret this conservatively.
Suggested safe validation settings:
- one job with `--distance-threads 8` or `12`
- avoid multiple concurrent heavy analyses unless you explicitly budget threads

### Sandboxing / commands
This environment often fails sandboxed commands with the familiar:
- `bwrap: No permissions to create a new namespace...`

So in practice:
- important shell commands often need escalation
- don't waste time retrying many safe-read commands inside sandbox if they matter

### Worktree hygiene
The worktree contains **many unrelated pre-existing uncommitted files**.
Do **not** blindly commit everything.
Only stage the files relevant to this slice.

Relevant selector slice files are only:
- `nad/core/selectors/code_dynamic_impl.py`
- `plugins/prefix_saturation_selector.py`
- optionally a tiny doc note if needed

## 6. Most important tactical guidance for next Codex

The next Codex should **not** re-open the whole research question.
That part is already settled enough for this slice.

The next Codex should do exactly this:
1. read the current handoff + analysis docs
2. inspect `code_dynamic_impl.py` and `prefix_saturation_selector.py`
3. verify they are correct and minimal
4. run a **targeted coding-only validation** on `livecodebench_v5`
5. compare the plugin against at least:
   - `min-confidence`
   - `tournament-copeland`
   - current prefix plugin behavior / old prefix-saturation baseline if recoverable
6. summarize whether the new code-oriented selector improves coding `SelAcc@10`
7. only then commit the selector work

## 7. Validation target to use

Use this coding cache:
- `MUI_HUB/cache/DeepSeek-R1-0528-Qwen3-8B/livecodebench_v5/cache_neuron_output_1_act_no_rms_20251127_032808`

Likely validation flow:
- `nad.cli analyze`
- then `nad.cli accuracy`

Selectors to compare at minimum:
- `min-confidence`
- `tournament-copeland`
- `file:/home/jovyan/work/NAD_Next/plugins/prefix_saturation_selector.py:PrefixSaturationSelector`

Keep evaluation coding-only first.
Do **not** expand to full cross-domain matrix in this handoff slice.

## 8. Current process state

I stopped the old stale CPU-heavy process:
- old PID `162277`
- it was a long-running old `nad.cli analyze` background job unrelated to the new selector slice

So the next session should start from a cleaner CPU state.

## 9. Recommended next commit strategy

If validation is good, make a small commit only for this slice, something like:
- `selectors: add code-oriented dynamic prefix/tail plugin`

Do not include unrelated local files.

## 10. Bottom line

The next unit of work is **not** "more analysis" and **not** "graph features".
It is:
- finish the code-oriented selector implementation already started
- validate it on `livecodebench_v5`
- decide whether it beats the current coding baselines on `SelAcc@10`
